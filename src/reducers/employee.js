const employees = [
    'Tiffany',
    'Sara',
    'Tyler',
    'Jack'
]

const employeesReducer = (state = employees, action) => {
    switch (action.type) {
      default:
        return state
    }
  }

export default employeesReducer