const employees = [
    'Tiffany',
    'Sara',
    'Tyler',
    'Jack',
    'Ben',
    'Chole'
]

const employeesReducer = (state = employees, action) => {
    switch (action.type) {
      default:
        return state
    }
  }

export default employeesReducer